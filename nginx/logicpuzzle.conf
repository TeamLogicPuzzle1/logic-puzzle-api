server {
    listen 80;
    server_name localhost;
    location / {
        uwsgi_pass        unix:///srv/logicpuzzle_web/logicpuzzle.sock;
        include           uwsgi_params;
    }
    location /static/ {
        alias /static/;
    }
}